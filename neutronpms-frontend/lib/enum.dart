enum SupportGroupType { zalo, telegram, facebook, youtube, gmail }

enum ChartType { bar, pie }

enum HotelPageStatus {
  loading,
  success,
  noHotel,
  updateInfo,
  needUpdateInfo,
  hotelNotFoundWithQuery
}
